apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: nova-commerce
  labels:
    app: api
spec:
  type: NodePort
  ports:
    - name: api
      port: 4000
      targetPort: 4000
  selector:
    app: api
  